var UserRegistration = function(mwy) {
    this.userName = mwy.userName,
    this.email = mwy.email,
    this.firstName = mwy.firstName,
    this.lastName = mwy.lastName,
    this.password = mwy.password,
    this.passwordConfirm = mwy.passwordConfirm
};
module.exports = UserRegistration;